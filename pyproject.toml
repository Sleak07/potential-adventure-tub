[project]
name = "term-tube"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bimal Mallick", email = "ghostridergothan8460@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "typer>=0.26.8",
    "yt-dlp>=2026.7.4",
]

[project.scripts]
term-tube = "term_tube:main"

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
